HOST=0.0.0.0
PORT=1337
APP_KEYS="toBeModified1,toBeModified2"
API_TOKEN_SALT=tobemodified
ADMIN_JWT_SECRET=tobemodified
ENCRYPTION_KEY=tobemodified
TRANSFER_TOKEN_SALT=tobemodified
JWT_SECRET=tobemodified

# Preview Configuration
PREVIEW_BASE_URL=http://localhost:3000
PREVIEW_SECRET=your-preview-secret-token
PREVIEW_ALLOWED_ORIGINS=http://localhost:3000

# Email (SMTP) Configuration
# Email Configuration (SendGrid Web API recommended)
EMAIL_PROVIDER=sendgrid
EMAIL_SENDER=contacts@tryenvoyx.com
EMAIL_REPLY_TO=contacts@tryenvoyx.com
SENDGRID_API_KEY=your-sendgrid-api-key

# (Optional) SMTP fallback
SMTP_HOST=smtp.sendgrid.net
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=apikey
SMTP_PASS=your-sendgrid-api-key
SMTP_REQUIRE_TLS=true
